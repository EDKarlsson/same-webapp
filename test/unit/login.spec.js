"use strict";

define('When in a user clicks login', function () {
    var scope = {};
    beforeEach(function () {
        module('same');
        inject(function($controller) {
            var
        });
    });
    it('take to login page', function () {
        expect('');
    });
});